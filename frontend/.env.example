# Base URL of the GoalVision backend API.
# Local dev uses the Vite proxy, so this can be left blank.
# In production (Vercel), set it to your Render backend URL, e.g. https://goalvision-api.onrender.com
VITE_API_BASE_URL=
